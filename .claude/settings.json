{
  "enabledPlugins": {
    "expo@claude-plugins-official": true
  },
  "autoCompactEnabled": true,
  "env": {
    "CLAUDE_AUTOCOMPACT_PCT_OVERRIDE": "65"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "br=$(git rev-parse --abbrev-ref HEAD 2>/dev/null); if [ \"$br\" = \"main\" ] || [ \"$br\" = \"master\" ]; then nb=\"feat/auto-$(date +%Y%m%d-%H%M%S)\"; git checkout -b \"$nb\" >/dev/null 2>&1 && echo \"{\\\"systemMessage\\\": \\\"Auto-created branch $nb (was on $br) before edit\\\"}\"; fi",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
